"""
Exporting Databricks analytics assets as CI/CD-ready YAML.

Genie spaces and Lakeview dashboards, written as downloadable bundles. Not to be
confused with ``services/export/`` (which packages a CREW as an app or notebook)
or with ``services/recipes/`` (which mines finished runs).
"""
