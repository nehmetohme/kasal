"""
Builders that turn stored configuration into engine objects.

Crew settings, the embedder block, and the manager-agent block. Separate from
``config_adapter`` (which normalizes the SHAPE of what the frontend sends)
because these build the real thing.
"""
