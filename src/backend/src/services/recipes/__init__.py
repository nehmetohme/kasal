"""
Workflow recipes: turning finished runs into reusable starting points.

``mining`` is EVENT-DRIVEN — the crew process executor calls it the moment a run
lands, so a recipe exists before anyone asks for one. ``recipes`` serves and
scores them.
"""
