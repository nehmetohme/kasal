/**
 * One-click instructions for the studio's bar. In their own module so the
 * bar's file exports only a component and Fast Refresh keeps its state while
 * the studio is being edited live.
 */
export const REFINE_CHIPS = [
  'Shorten the text',
  'Make it more visual',
  'Bigger title',
  'Turn the text into bullets',
  'Add speaker notes',
  'Change the layout',
];

export const FILL_CHIPS = ['An agenda', 'A summary of the previous slide', 'Next steps', 'A section divider'];
