{
  "name": "kasal-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci --include=dev && npm run build",
    "postbuild": "node scripts/build-tasks.cjs postbuild"
  }
}
